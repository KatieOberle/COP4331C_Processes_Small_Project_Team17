# Database

- schema.sql — table structure only (Users, Contacts)
- schema_with_data.sql — schema + test data
- ERD.png — entity relationship diagram (add once created)

## Connection info
Host: localhost
Database: ContactManager
App user: contactapp (password shared with team separately — not committed here)
